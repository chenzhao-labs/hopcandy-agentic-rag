export type Mode = 'replay' | 'live';
export type MobilePanel = 'answer' | 'evidence' | 'trace';
export type ReplayState = 'idle' | 'playing' | 'complete';
export type HeaderDataScope = { companies: string[]; years: number[]; corpus: string | null };
